## 10:50 — career-fit dev

- Done: Added `career-fit dev` CLI to start API (:8787) + Vite UI (:5173) together; README + AI_BUILD_MAP updated
- Decisions: Prefer one CLI entry over Makefile/concurrently for local dogfood
- Next: User dogfoods UI; then S6 tagging or JD normalizers

## 11:05 — job URL session map (Warm Bridge align)

- Done: `sources.yaml` yellow primary `linkedin_selenium_job`; ARCHITECTURE/AGENTS/skills; `linkedin_selenium` + `insights`; `/api/map-job`; Craft URL-first + Role insights; `map-job --mock`
- Decisions: Session Selenium (local Chrome) ≠ red harvest; paste demoted to fallback; mock for dogfood without Selenium extra
- Next: Live job-page DOM smoke; optional shared session yaml with warm-bridge

## 11:25 — roles yes/no + erase saved intake

- Done: `suggest_roles` + role cards after map-profile; Start **Fresh** / `?fresh=1` / Erase saved intake; Craft prefilled with first yes LinkedIn search URL
- Decisions: LinkedIn Jobs *search* links (real) until session can pin `jobs/view` cards; taps beat form fields
- Next: Dogfood Fresh → mock profile → yes/no roles → Craft

## 11:50 — journey dogfood harden

- Done: gitignore session/chrome paths; `data/linkedin_session.yaml` → WB chrome_profile; `.[linkedin]` installed; live Chrome miss → **503** (not 500); Start single CTA + silent mock fallback; **Fresh** always in nav; `POST /api/sample-pack` + CLI; roles Continue → Craft with pack filled; JOURNEY/CURRENT/AI_BUILD_MAP synced
- Decisions: Sample pack closes first-value loop without JD paste; live session never blocks dogfood
- Next: Browser dogfood `/?fresh=1`; live when Linux Chrome + WB profile exist

## 15:45 — jobs-first + honest LinkedIn fail

- Done: Removed silent demo-as-you; stub “link only” (name from URL); Start steps Link→Roles→**Jobs**→optional; `suggest-openings`; pick job → tailor & save / save only / Craft; Pro/Free UI omitted; recruiters collapsed until pack exists; save-application allows role-only
- Decisions: Job pick before tailor; never invent IQVIA path as Northwind; monetization UI off while dogfooding
- Next: User retries real LinkedIn URL → Continue with this link only → roles → jobs → tailor

## 16:10 — real jobs only + Chrome session

- Done: Chrome-for-Testing in `.browser/`; `data/chrome_profile`; `session-status` / `linkedin-session` / `search-jobs`; live `suggest-openings` scrapes real `/jobs/view` cards and **skips incomplete**; Northwind sample openings removed from journey
- Decisions: Complete live postings only; 503 → run linkedin-session (not fake cards)
- Next: User runs `career-fit linkedin-session`, then Fresh E2E with real profile + real jobs

## 16:50 — wedge sticky: session UI + aha

- Done: Start session banner (`/api/session-status`); `POST /api/linkedin-session` + Open login CTA + Copy CLI; Retry live after login; empty search → paste `jobs/view` first (no mock fallback on paste map); Craft opens company message after tailor&save; Today “Send outreach for {company}”
- Decisions: Session unlock in-product before more scrape; aha = message to send a human
- Next: Founder Fresh E2E — Open login → real profile → real jobs → tailor&save → Home

## 17:05 — no LinkedIn login path

- Done: Start rewritten to résumé/YAML → roles → paste JD (company required); LinkedIn session off happy path; `sources.yaml` paste primary; JOURNEY/CURRENT updated
- Decisions: Owner will not log into LinkedIn for dogfood — green paste is the wedge
- Next: Fresh E2E with real résumé + real JD paste → message on Craft / Today

## 17:10 — URL-only Start (kill form friction)

- Done: Start back to **LinkedIn URL only** (stub, no login); résumé questionnaire removed from happy path; deepen stays optional
- Decisions: One URL → roles → paste JD; never grill for bio on Start
- Next: Fresh → paste `/in/…` → roles → JD → tailor&save

## 17:15 — jobs first + one-shot job URL scrape

- Done: Jobs step shows openings/browse cards first; only ask is a **job URL**; `public_fetch` scrapes without LinkedIn login; title/company/JD fields removed
- Decisions: User always pastes links; we scrape; never multi-field job forms
- Next: Fresh E2E — pick or paste jobs/view or careers URL → tailor

## 17:20 — fix search?currentJobId= scrape

- Done: Normalize LinkedIn search URLs with `currentJobId` → `/jobs/view/{id}`; guest LinkedIn HTML parse for title/company/JD
- Next: Re-paste same search URL → Use URL → should select Hire Feed / JS Frontend
