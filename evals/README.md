# Evals

Fast, local checks — no API key required for classification cases.

```bash
python -m career_fit eval
```

Classification uses the deterministic keyword scorer in `career_fit.angle`.
LLM polish / full tailor checks can be added later behind an optional provider.
